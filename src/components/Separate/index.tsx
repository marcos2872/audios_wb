import React from 'react';
import { View } from 'react-native';
import { stylesSeparate } from './styles.Separete';

const Separate: React.FC = () => {
  return <View style={stylesSeparate.container} />;
}

export default Separate;